import { Container } from "typedi";

export async function initIoC() {
  // const cfg = await service.initIoC(join(process.cwd(), 'src'));

  // const dbFactory = Container.get<DbFactoryBase>(DbFactoryBase as any);

  // const redis = Container.get<RedisBase>(RedisBase as any);
  // const nowTime = Container.get<NowTimeBase>(NowTimeBase as any);
  // const offsetNowTime = new OffsetNowTime(nowTime, redis);
  // Container.set(NowTimeBase, offsetNowTime);

  // Container.set(ActivityManagerBase, new ActivityManager(dbFactory));

  // return cfg;
}